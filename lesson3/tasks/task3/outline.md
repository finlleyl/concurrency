# Обработка ошибок через канал ```Result```

Создай структуру ```Result{data int, err error}```.

Пусть функция возвращает ошибку для чётных чисел.

Потребитель должен распечатать ```data```, а ошибку — вывести через ```log```.