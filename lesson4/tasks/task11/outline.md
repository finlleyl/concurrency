## Worker Pool с ошибками

Модифицируй воркера, чтобы он возвращал ошибку при j == 5.
Реализуй сбор ошибок через ```errgroup.Group```, отмену всех
остальных воркеров при первой ошибке и вывод “первой” 
ошибки в main.