Флаг занятости (isBusy)

Используй int32 как флаг: 0 = свободен, 1 = занят.

Одна горутина ставит 1, если флаг равен 0. Иначе — ничего не делает.

Покажи, как можно построить атомарный mutex без ```sync.Mutex```.